package com.ecommercestore.backend.order;

public enum OrderItemStatus {
    RESERVED,
    ORDERED,
    CANCELLED,
    EXPIRED,
    REFUNDED,
    PARTIALLY_REFUNDED,
    RETURN_REQUESTED,
    RETURNED,
    DAMAGED
}
